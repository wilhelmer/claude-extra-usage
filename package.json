{
  "name": "claude-extra-usage",
  "displayName": "Claude Extra Usage",
  "repository": "https://github.com/wilhelmer/claude-extra-usage",
  "description": "Displays Claude extra credit usage as a percentage in the VS Code status bar.",
  "author": "Lars Wilhelmer",
  "publisher": "LWilhelmer",
  "version": "0.0.3",
  "icon": "icon.png",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": ["Other"],
  "activationEvents": ["onStartupFinished"],
  "main": "./out/extension.js",
  "contributes": {
    "configuration": {
      "title": "Claude Extra Usage",
      "properties": {
        "claudeExtraUsage.refreshInterval": {
          "type": "number",
          "default": 10,
          "minimum": 1,
          "description": "How often usage data should be refreshed (in minutes)."
        }
      }
    },
    "commands": [
      {
        "command": "claudeExtraUsage.refresh",
        "title": "Claude Extra Usage: Refresh Now"
      },
      {
        "command": "claudeExtraUsage.configure",
        "title": "Claude Extra Usage: Configure"
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/vscode": "^1.85.0",
    "typescript": "^5.3.0"
  }
}
